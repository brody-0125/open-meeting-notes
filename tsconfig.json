{
  // ponytail: noCheck emit until JS-shaped classes get field types. Syntax errors still fail.
  "compilerOptions": {
    "strict": true,
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "target": "ES2022",
    "lib": ["ESNext", "DOM"],
    "types": ["node"],
    "noCheck": true,
    "noEmitOnError": true,
    "skipLibCheck": true,
    "declaration": false
  },
  "include": [
    "src/**/*.mts",
    "src/**/*.cts",
    "test/**/*.mts",
    "test/**/*.cts",
    "scripts/**/*.mts"
  ]
}
